// Bump via `pnpm run version:bump [patch|minor|major]`. The bump script
// rewrites this file and the matching VERSION line in public/sw.js so the
// service-worker cache name updates in lockstep with the displayed version.
export const APP_VERSION = "0.2.27";
